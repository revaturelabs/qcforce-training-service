/**
 * Provides the service interfaces and classes which implement methods related
 * to Revature batches, and their corresponding employees and associates.
 * <p>
 * The service package comprises four interfaces and four classes which
 * implement each respective interface: the {@link AssociateService} interface
 * and {@link AssociateServiceImpl} class, the {@link BatchService} interface
 * and {@link BatchServiceImpl} class, the {@link EmployeeService} interface and
 * {@link EmployeeServiceImpl} class, and the {@link EmployeeBatchService}
 * interface and {@link EmployeeBatchServiceImpl} class. Each interface/class
 * pair is also associated with the {@code Associate}, {@code Batch},
 * {@code Employee}, and {@code EmployeeBatch} classes from the
 * {@code com.revature.model} package.
 *
 * @since 1.0
 * @see com.revature.model
 */
package com.revature.service;